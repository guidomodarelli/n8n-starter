# FQDN (Fully Qualified Domain Name): the full hostname that identifies this service on the network
# (host + domain). Used for generating URLs, routing, and TLS certificates (e.g. n8n.localhost or n8n.example.com).
FQDN=n8n.localhost

# Optional timezone to set which gets used by Cron and other scheduling nodes
# New York is the default value if not set
# See https://stackoverflow.com/a/72218601
GENERIC_TIMEZONE=America/Argentina/Buenos_Aires

# The email address to use for the TLS/SSL certificate creation
SSL_EMAIL=example@gmail.com

# Cloudflare Tunnel token to enable Cloudflare Tunnel for secure access without opening ports
CF_TUNNEL_TOKEN=